name: Latest blog post workflow
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
permissions:
  contents: write

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4  # ← 升级到 v4 (支持 Node.js 24)
      
      - name: Pull in WinJay's posts
        uses: gautamkrishnar/blog-post-workflow@master  # ← 用 master 分支 (最新版)
        with:
          max_post_count: 6
          committer_email: "github-actions[bot]@users.noreply.github.com"
          feed_list: "https://winjay.cn/rss.xml"
          committer_username: "github-actions[bot]"
          template: "$newline- $randomEmoji(💯,🔥,💫,🚀,🌮,📝,🥳,💻,🧰,🏊,🥰,🧐,🤓,😎,🥸,🤩,🤗,🤔,🫣,🤭,🤠,👹,👺,🤡,🤖,🎃,😺,🫶,👍,💪,💄,👀,🧠,🧑‍🏫,👨‍🏫,💂,🧑‍💻,🥷,💃,🕴,💼,🎓,🐻,🐵,🙉,🦄,🦆,🦅,🦍,🦣,🐘,🦒,🦏,🐎,🦩,🐲,🌝,🌜,🌏,🌈,🌊,🎬,🎭,🚀,🚦,⛽️,🗽,🎡,🌋,🌁,💡,🕯,🪜,🧰,⚗️,🔭,🪄,🎊,🎉,) [$title]($url) $newline"
          retry_count: 3  # ← 新增：重试 3 次
          request_timeout: 60  # ← 新增：超时 60 秒
